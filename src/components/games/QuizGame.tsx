"use client";
import { useMemo,useState } from "react";
import { AppIcon } from "@/components/AppIcon";
import { shuffle } from "@/lib/game-engine";
import type { GameProps } from "./GameTypes";
import { CompletionCard } from "./CompletionCard";
export function QuizGame({activity,onComplete}:GameProps){const[questions,setQuestions]=useState(()=>shuffle(activity.items));const[index,setIndex]=useState(0);const[correct,setCorrect]=useState(0);const[score,setScore]=useState(0);const[streak,setStreak]=useState(0);const[selected,setSelected]=useState<string|null>(null);const[finished,setFinished]=useState(false);const item=questions[index];const options=useMemo(()=>item?shuffle([item.answer,...shuffle(activity.items.filter(c=>c.id!==item.id).map(c=>c.answer)).slice(0,3)]):[],[activity.items,item]);function choose(option:string){if(selected)return;setSelected(option);const right=option===item.answer;const nextStreak=right?streak+1:0;const points=right?100+Math.min(nextStreak,5)*20:0;const nc=correct+(right?1:0),ns=score+points;setStreak(nextStreak);setCorrect(nc);setScore(ns);setTimeout(()=>{if(index===questions.length-1){setFinished(true);onComplete(ns,nc,questions.length)}else{setIndex(v=>v+1);setSelected(null)}},900)}function replay(){setQuestions(shuffle(activity.items));setIndex(0);setCorrect(0);setScore(0);setStreak(0);setSelected(null);setFinished(false)}if(finished)return <CompletionCard score={score} correct={correct} total={questions.length} onReplay={replay}/>;return <div className="game-stage choice-stage"><div className="game-progress-label"><span>Question {index+1} of {questions.length}</span><span><AppIcon name="fire" /> {streak} streak · {score} pts</span></div><div className="game-progress"><span style={{width:`${((index+1)/questions.length)*100}%`}}/></div><div className="game-question"><span className="question-icon">{item.hint||<AppIcon name="trophy" />}</span><small>QUICK QUIZ</small><h2>What does <em>“{item.prompt}”</em> mean?</h2></div><div className="choice-grid">{options.map((option,i)=><button key={`${option}-${i}`} onClick={()=>choose(option)} className={selected?option===item.answer?"correct":selected===option?"wrong":"dimmed":""}><span>{String.fromCharCode(65+i)}</span>{option}</button>)}</div>{selected&&<div className={`feedback-message ${selected===item.answer?"correct":"wrong"}`}>{selected===item.answer?<><AppIcon name="check-lg" /> Correct!</>:`The answer is “${item.answer}”.`}</div>}</div>}
